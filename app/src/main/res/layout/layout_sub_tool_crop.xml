<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/surface"
    android:orientation="vertical"
    android:paddingTop="12dp"
    android:paddingBottom="20dp">

    <!-- Đường gạch nhỏ trang trí trên đầu Bottom Sheet -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:background="#E0E0E0" />

    <!-- Danh sách các tỷ lệ Crop (1:1, 3:4, 9:16...) kéo ngang -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_crop_ratios"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

    <!-- Nút xác nhận thực hiện lệnh Cắt -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_apply_crop"
        style="@style/Widget.Material3.Button.TonalButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"
        android:text="Áp dụng"
        android:textColor="@color/primary"
        app:backgroundTint="#FFF5F7"
        app:cornerRadius="20dp"
        app:icon="@drawable/ic_crop"
        app:iconTint="@color/primary" />

</LinearLayout>